# strkit — Go acceptance dogfood fixture

Pure-Go string utilities (module `example.com/strkit`). Used to validate that the
Syndai acceptance-criteria compiler binds red-by-absence validators for Go.
